# Invariance SDK Configuration
INVARIANCE_PRIVATE_KEY=          # Hex private key for the agent wallet
INVARIANCE_RPC_URL=              # Base RPC endpoint (e.g., https://sepolia.base.org)
INVARIANCE_CHAIN=base-sepolia    # "base" or "base-sepolia"
INVARIANCE_API_KEY=              # Optional: for managed-mode hosting

# Wallet Encryption (for persistent wallet storage)
INVARIANCE_WALLET_PASSWORD=      # Password to encrypt/decrypt stored wallet
